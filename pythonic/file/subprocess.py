# subprocess 사용해 터미널 명령어를 사용해보자.

import subprocess

# subprocess.run(['ls', '-al'])
# subprocess.run('ls -al | grep test', shell=True)

# cmd = 'dasdasfdsar | rm -rf *'
# subprocess.run(cmd, shell=True)
